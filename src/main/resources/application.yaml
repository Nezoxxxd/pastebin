server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pastebin_db
    username: root
    password: tQzeLv6w
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    generate-ddl: true
    show-sql: true
    properties:
      hibernate:
        format_sql: true

